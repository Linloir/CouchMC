# Generated by xcodegen — regenerate via `xcodegen generate` from project.yml.
McController.xcodeproj/

# Xcode local user state
**/xcuserdata/

# Swift Package Manager / build artefacts
.build/
.swiftpm/
DerivedData/

# Generated icon PNGs are checked in (so Xcode can compile the asset
# catalogue without running the script first), but a developer who
# re-runs the script should NOT see local image edits as a diff: the
# script is deterministic and the output is stable.

# macOS clutter
.DS_Store
