<?xml version="1.0" encoding="utf-8"?>
<!--
    Host card v3. Clean four-row composition:
      1. Title + trailing overflow icon button (M3 standard secondary
         action surface). Tapping the icon opens a PopupMenu with
         rename / change port / forget.
      2. Description (IP · port), gray.
      3. Status row — small coloured dot on the left, status word in the
         matching colour next to it. No pill background; the dot + text
         carry the colour signal cleanly.
      4. Last-connected line — relative time, or "Not connected yet"
         if there's no history.

    Card chrome: solid surface, 22 dp corners, 3 dp drop shadow, no
    border. Horizontal padding matches the title's typographic weight
    (22 dp) — feels roomy without ballooning the card height.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/host_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="16dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="?attr/colorSurface"
    app:cardCornerRadius="22dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp"
    style="@style/Widget.Material3.CardView.Elevated">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="22dp"
        android:paddingTop="18dp"
        android:paddingBottom="20dp">

        <!-- Row 1 — title + trailing overflow -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:baselineAligned="false">

            <TextView
                android:id="@+id/host_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="?attr/textAppearanceHeadlineSmall"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:textSize="24sp"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="JonDesk" />

            <ImageButton
                android:id="@+id/host_overflow"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginEnd="-8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_more_vert"
                app:tint="?attr/colorOnSurfaceVariant"
                android:contentDescription="@null" />

        </LinearLayout>

        <!-- Row 2 — description (IP · port) -->
        <TextView
            android:id="@+id/host_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="192.168.0.101 · 34555" />

        <!-- Row 3 — status: coloured dot + matching-coloured word -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/host_status_dot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:src="@drawable/ic_status_dot"
                app:tint="@color/status_online"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/host_status_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textAppearance="?attr/textAppearanceLabelLarge"
                android:textColor="@color/status_online"
                android:textStyle="bold"
                tools:text="Available" />

        </LinearLayout>

        <!-- Row 4 — last-connected -->
        <TextView
            android:id="@+id/host_last_connected"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="Last connected 2 hr ago" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/host_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_gravity="bottom"
        android:indeterminate="true"
        android:visibility="gone" />

</com.google.android.material.card.MaterialCardView>
