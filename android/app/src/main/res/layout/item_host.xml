<?xml version="1.0" encoding="utf-8"?>
<!--
    Compact host card. Three rows, tight rhythm:

      1. Title (17 sp bold) + trailing overflow icon button.
      2. Description line — `ip · port · ● status` rendered as one
         TextView; the status portion (bullet + word) is coloured + bold
         via a SpannableString built in the adapter.
      3. Last-connected (or "Not connected yet"), small / muted.

    Outlined Material 3 card, 16 dp corners, no shadow — matches the
    Settings page chrome exactly. Top & bottom padding made symmetric
    after the user reported the top edge feeling heavier than the
    bottom.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/host_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:layout_marginHorizontal="16dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="?attr/colorSurface"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="?attr/colorOutlineVariant"
    app:strokeWidth="1dp"
    style="@style/Widget.Material3.CardView.Outlined">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:paddingVertical="14dp">

        <!-- Row 1 — title + trailing overflow -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:baselineAligned="false">

            <TextView
                android:id="@+id/host_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="?attr/textAppearanceTitleMedium"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:textSize="17sp"
                android:ellipsize="end"
                android:maxLines="1"
                android:includeFontPadding="false"
                tools:text="JonDesk" />

            <!--
                Height is pinned to the icon's natural 24 dp instead of the
                Material-default 36–48 dp because the parent row uses
                gravity="center_vertical": a taller ImageButton forces the
                row taller than the title TextView, which then puts ~7 dp
                of empty space above the title (within paddingTop) and
                broke the visual symmetry with the bottom padding. Width
                stays at 36 dp + a 4 dp negative end-margin so the
                horizontal touch target / visual position is unchanged.
            -->
            <ImageButton
                android:id="@+id/host_overflow"
                android:layout_width="36dp"
                android:layout_height="24dp"
                android:layout_marginEnd="-6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_more_vert"
                app:tint="?attr/colorOnSurfaceVariant"
                android:contentDescription="@null" />

        </LinearLayout>

        <!--
            Row 2 — description + inline status. The status portion is a
            colour-spanned suffix (see HostListAdapter.buildDescription).
        -->
        <TextView
            android:id="@+id/host_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="192.168.0.101 · 34555 · ● Available" />

        <!-- Row 3 — last-connected (or "Not connected yet") -->
        <TextView
            android:id="@+id/host_last_connected"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="Last connected 2 hr ago" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/host_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_gravity="bottom"
        android:indeterminate="true"
        android:visibility="gone" />

</com.google.android.material.card.MaterialCardView>
